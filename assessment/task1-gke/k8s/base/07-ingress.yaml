apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hirelink-ingress
  namespace: hirelink-prod
  annotations:
    kubernetes.io/ingress.class: "gce"
    kubernetes.io/ingress.global-static-ip-name: "hirelink-ingress-ip"
    networking.gke.io/managed-certificates: "hirelink-managed-cert"
    networking.gke.io/v1beta1.FrontendConfig: "hirelink-frontendconfig"
spec:
  ingressClassName: gce
  rules:
    - host: app.niranjan.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hirelink-web
                port:
                  number: 80

    - host: api.niranjan.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hirelink-api
                port:
                  number: 8000