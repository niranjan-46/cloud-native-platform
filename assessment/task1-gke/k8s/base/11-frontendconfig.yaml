apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: hirelink-frontendconfig
  namespace: hirelink-prod
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
