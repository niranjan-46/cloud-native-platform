import PublicJobsPage from "../components/PublicJobsPage";

export const metadata = {
  title: "Discover Verified Job Opportunities | HireLink",
  description: "Search high-quality openings from trusted employers and apply with a profile designed for faster screening.",
};

export default function JobsPage() {
  return <PublicJobsPage />;
}
